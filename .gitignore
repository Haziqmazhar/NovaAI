venv/
__pycache__/
*.pyc

# Local Claude Code tooling config, not project source.
.claude/

# Downloaded separately per README — large binary model files, not source.
model/

# Never commit secrets.
.env
*.key

# Nova's own generated memory file (default path, config.json's
# memory_path) — runtime data with real personal notes, not source.
memory.md
config.json.bak
